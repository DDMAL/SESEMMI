from app.graph.examples import FEW_SHOT_EXAMPLES

# RAG corpus — the dataset embedded into the vector store.
# Starts from the 24 static examples; extend this list to improve retrieval.
RAG_CORPUS = FEW_SHOT_EXAMPLES
